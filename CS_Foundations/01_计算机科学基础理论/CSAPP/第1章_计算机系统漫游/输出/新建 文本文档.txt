四、进程
 
操作系统：对正在运行的程序实例的抽象
 
核心组成：
 
- 指令：可执行代码程序
- 全局变量：程序数据
- 运行时栈：函数调用、局部变量
- 程序计数器 PC 和寄存器状态（上下文）
 
操作系统通过上下文切换在进程之间快速切换，让用户感觉多个程序同时在进行。单核CPU上，同一时刻只有一个进程在运行，但切换的速度极快，看起来是并行的。
 
操作系统保持跟踪进程运行所需的所有状态信息。这种状态，也就是上下文，包括许多信息，比如PC和寄存器文件的当前值，以及主存的内容。当操作系统决定要把控制权从当前进程转移到某个新进程时，就会切换上下文切换，即保存当前进程的上下文、恢复新进程的上下文，然后将控制权传递到当前新进程。新进程就会从他上次停止的地方开始。
 
- 内核（kernel）：操作系统代码常驻主存的部分，系统管理全部进程所用代码和数据结构的集合。（似懂）
 
事件 read
进程A
进程B
return
磁盘中断
 
A：内核代码
B：用户代码
 
进程的上下文切换（不懂，没理解，不能输出）
 
用户
进程A read
进程B
 
- 文件：字节序列，仅此而已。
 

\begin{align*}
T_{oldpart} &= \alpha \cdot T_{old} + \overline{\alpha} \cdot T_{inspart} \quad \alpha = \frac{1}{T_{inspart}} \\
T_{newpart} &= R \\
T_{newpart} &= k \cdot T_{trans} = k(\alpha \cdot T_{old}) \\
T_{new} &= (1-\alpha)T_{old} + T_{newpart} \\
&= (1-\alpha)T_{old} + \alpha k T_{old} \\
T_{new} &= T_{old}(1-\alpha+\alpha k) \\
\therefore \frac{T_{new}}{T_{old}} &= \frac{1-\alpha+\alpha k}{1} = \frac{1}{s}
\end{align*}

 
- 并发：系统同时具有多个活动
- 并行：用并发使一个系统运行得更快